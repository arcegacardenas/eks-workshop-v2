Version: "2012-10-17"
Statement:
  - Effect: Allow
    Action: ssm:CreateActivation
    Resource:
      - !Sub arn:aws:iam::${AWS::AccountId}:role/${Env}-hybrid-node-role-*
      - !Sub arn:aws:iam::${AWS::AccountId}:role/${Env}-devops-agent-ssm-hybrid
