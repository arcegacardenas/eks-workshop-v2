#!/bin/bash

set -e

# The DevOps Agent setup module is educational only.
# No resources to clean up — the CLI model patch is harmless to leave.

logmessage "DevOps Agent setup module cleanup complete."
